from setuptools import setup, find_packages

setup(
    name = 'postSPIT',
    description = 'Package used to do post-processing of SPIT results an to plot the data genereated by SPIT and by this same package',
    version = '0.1',
    packages = find_packages(),
    author= 'Gerard Castro-Linares',
    url='',
    classifiers=[
        "Programming Language :: Python :: 3.7",
        "Operating System :: OS Independent",
   ],
   
)
